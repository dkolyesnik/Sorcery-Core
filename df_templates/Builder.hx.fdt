package $(Package);

import sorcery.framework.builder.BuilderEvent;
import sorcery.framework.builder.BuilderComponent;

/**
$(CBI)* ...
$(CBI)* @author $(DefaultUser)
$(CBI)*/
class $(FileName) extends BuilderComponent $(CSLB){

	override function onBuild(e:BuilderEvent):Void $(CSLB){
		var entity = e.entity;
		switch(e.entityId)
		{
			$(EntryPoint)
		}
	}
}